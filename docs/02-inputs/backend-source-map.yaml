schema_version: 1
backend:
  type: Go backend
  owner: unknown
  official_semantic_version: not_supplied
  compatibility_placeholder: 0.0.0
  version_status: contract_documented_version_unassigned
  contract_revision: 1
  openapi:
    destination: docs/05-backend/openapi.yaml
    status: documented_pending_backend_owner_review
  environments:
    local:
      base_url: "http://localhost:8080"
      status: supplied_not_verified
    production:
      base_url: unknown
      status: not_supplied
  operations:
    - method: POST
      path: /api/lead
      status: contract_documented
      request_schema: LeadRequest
      response_schema: LeadSuccessResponse
      http_status_codes: [200, 400, 500, 502]
      authentication: unknown
      errors:
        - invalid JSON request body
        - workEmail must be valid
        - internal server error
        - failed to create contact in HubSpot
      processing:
        - backend
        - HubSpot
        - email
notes:
  - The official backend semantic version has not been supplied.
  - Compatibility placeholder 0.0.0 is not a backend release.
  - Contract revision 1 must not be interpreted as semantic version 1.0.0.
  - The OpenAPI contract is now documented at the registered destination.
  - Backend-owner review and official semantic version assignment remain pending.
